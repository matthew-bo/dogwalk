{
  "name": "shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Dog Walk Gamble",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "rimraf": "^5.0.0",
    "@types/node": "^18.15.11"
  }
} 